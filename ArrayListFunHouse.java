//(c) A+ Computer Science
//www.apluscompsci.com
//Name -

import java.util.Scanner;
import java.util.List;
import java.util.ArrayList;
import java.util.Collections;
import static java.lang.System.*;

public class ArrayListFunHouse
{
 public static ArrayList<Integer> getListOfFactors(int number)
 {
  /*
 //create a for loop that initializes integer i = 2. It will run through every integer until it reaches the parameter number.
 for(int i = 2; i < number; i++)
 {
   //create an if loop that will check if the remainder is 0 when number is divided by i.
   if(number % i == 0)
   {
     //if the remainder is zero, then i will be added to the arrayList.
     ArrayListFunHouse.add(i);
   }
//continue until all values have been tested

 }
//exit the if and for loop and print the final arrayList.
System.out.println(ArrayListFunHouse());
*/
return null;
 }
 
 public static void keepOnlyCompositeNumbers( List<Integer> nums )
 {
 }
}